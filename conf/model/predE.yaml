_target_: dao.pl_modules.energy_modeo.IEPModule
time_dim: 256
latent_dim: 0
cost_coord: 1.
cost_lattice: 1.
cost_atom: 1.
cost_scalar: 1.
max_neighbors: 20  # maximum number of neighbors for OTF graph bulding in decoder
radius: 7.  # maximum search radius for OTF graph building in decoder
timesteps: 1000
pretrain_repr: 

defaults:
  - decoder: crysformer
  - beta_scheduler: cosine
  - sigma_scheduler: wrapped
